import 'package:flutter/material.dart';

class AppColors {
  //  User (Orange Palette)
  // الأغمق: للأزرار الرئيسية والنصوص المهمة
  static const Color userDeepDark = Color(0xFFE65100);
  // الأساسي: اللون اللي اخترته أنت (Price Catch Brand)
  static const Color userPrimary = Color(0xFFFF8A00);
  // الأفتح: للخلفيات الخفيفة أو الـ Hover effects
  static const Color userLight = Color(0xFFFFF3E0);

  //  Seller (Grey Palette)
  // الأغمق: للخلفيات الرسمية أو التبويبات الغامقة (تنسيق فخم)
  static const Color sellerDeepDark = Color(0xFF212121);
  // الأساسي: رمادي متزن للوحة تحكم التاجر
  static const Color sellerPrimary = Color(0xFF424242);
  // الأفتح: لإطارات الجداول أو حقول الإدخال عند التاجر
  static const Color sellerLight = Color(0xFFEEEEEE);

  //  Common (Shared)
  static const Color backgroundLight = Color(
    0xFFF5F7FA,
  ); // خلفية التطبيق العامة
  static const Color errorRed = Color(0xFFD32F2F); 
  static const Color successGreen = Color(0xFF388E3C); 
}
