class AuthContexts {
  static int loginItems = 3; 
  // email + password + button

  static int signupUserItems = 4; 
  // name + email + password + button

  static int signupSellerItems = 5; 
  // store name + phone + email + password + button
}